meta_schema_note: "Lightweight YAML schema for Codex and validator hints, not full JSON Schema."
meta_schema_note_zh: "轻量 YAML schema，用于 Codex 和 validator 提示，不是完整 JSON Schema。"

required: [decision_id, source_refs, evidence, feasible_surfaces, rejected_surfaces, selected, rationale_zh]
properties:
  decision_id: string
  selected.surface_type: string
  selected.attachment: string_optional
  rationale_zh: string
