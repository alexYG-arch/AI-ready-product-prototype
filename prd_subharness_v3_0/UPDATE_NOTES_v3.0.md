# Update Notes v3.0

- Added `DRD_MODE` as prototype harness default projection mode.
- Integrated `prototype_harness_drd_codex_package_v3_0` under `sub_harnesses/prototype_projection_harness/drd/`.
- Added source slices, materialization manifest, board/page shards, host surface inference, component primitive libraries, Dev Mode annotations and Pen logic line rules.
- Skipped real Figma `node.reactions` in DRD mode.
- Added v3.0 validator compatibility and DRD-specific `prototypectl.py` validation commands.
- Added `patch_control_v3.yaml` while preserving `patch_control_v2.yaml` for compatibility.
