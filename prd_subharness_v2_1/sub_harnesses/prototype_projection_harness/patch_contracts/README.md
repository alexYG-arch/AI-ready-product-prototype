# Prototype Projection Patch Contracts

- `PROTO-008-runtime-model.yaml`: 生成或校验 prototype runtime。
- `PROTO-009-figma-forward-projection.yaml`: runtime → Figma frame / reaction / flow map。
- `PROTO-010-figma-diff-ingest.yaml`: Figma diff → patch candidate。
- `PROTO-011-interaction-graph.yaml`: interaction graph 和 Figma reaction map 的一致性合同。
